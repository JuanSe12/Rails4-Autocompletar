<h1>Lista de marcas</h1>
<p>Aqui estan las marcas que se autocomplementan en los labels.</p>


<table  class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3">Opciones</th>
    </tr>
  </thead>

  <tbody>
    <% @brands.each do |brand| %>
      <tr>
        <td><%= brand.name %></td>
        <td><%= link_to 'Ver', brand %></td>
        <td><%= link_to 'Editar', edit_brand_path(brand) %></td>
        <td><%= link_to 'Eliminar', brand, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva marca', new_brand_path %><hr>
<ul class="nav nav-pills nav-stacked" style="max-width: 300px;">
  <li class="active"><%= link_to "Volver", controller: "products", action: "index" %></li>
</ul>